version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 16
        - npm ci
    build:
      commands:
        - nvm use 16
        - node -v
        - npm run build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*